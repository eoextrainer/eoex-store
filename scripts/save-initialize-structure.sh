#!/bin/bash
# Save and push the initial project structure for EOEX Store
# Usage: ./scripts/save-initialize-structure.sh

../eoex-store-saves.sh "Initialize project structure: frontend, backend, database, docs, scripts, .gitignore"
