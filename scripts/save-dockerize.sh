#!/bin/bash
# Save and push the Dockerization stage for EOEX Store
# Usage: ./scripts/save-dockerize.sh

../eoex-store-saves.sh "Dockerize all services: frontend, backend, database, docker-compose.yml"
