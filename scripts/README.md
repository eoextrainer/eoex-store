# eoex-app-store-01 Scripts

This folder contains workflow automation scripts.

- `app-changes.sh`: Automate git add, commit, and push at each implementation stage.

## Usage
```sh
./scripts/app-changes.sh "commit message"
```
